[
  {
    "task": "Parse an AWS S3 bucket URI and extract the bucket name and object key",
    "format": "python"
  },
  {
    "task": "Validate that a string matches the AWS IAM role ARN format (arn:aws:iam::account-id:role/role-name)",
    "format": "regex"
  },
  {
    "task": "Create a JSON policy document that allows EC2 instances to read objects from a specific S3 bucket",
    "format": "json"
  }
]